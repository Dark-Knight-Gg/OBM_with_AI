# Feature Mapping — Add Contract

> **Feature:** `add_contract`
> **Source Design:** Stitch export (`add_contract_design_stitch/`)
> **Generated by:** `/vnpt_workflow_stitch2flutter`
> **Date:** 2026-07-02
> **Status:** Approved

---

## 1. INPUTS

### 1.1 SRS Reference

| Field | Value |
|----|----|
| SRS File | `lib/features/add_contract/add_contract_srs/OBM_add_contract_srs.docx` |
| Version | 1.0 |
| Date | 2026-06-29 |
| Owner | DAS Team |

### 1.2 Stitch Design Export

| Field | Value |
|----|----|
| HTML | `lib/features/add_contract/add_contract_design_stitch/code.html` |
| CSS YAML | `lib/features/add_contract/add_contract_design_stitch/DESIGN.md` |
| Design System | Corporate Blue Clarity |

### 1.3 Postman

| Field | Value |
|----|----|
| Collection | OBM (`40297950-c828d846-f288-4af3-a973-2f249e6d648a`) |
| Endpoint | `POST http://10.168.6.37:9080/contracts` (AddContract) |
| Filter APIs | `dashboard/filter-options`, `opportunities`, `projects`, `customers`, `product-services` |

---

## 2. DESIGN → CODE MAPPING

### 2.1 Screen Mapping

| Stitch Section | Code Section |
|---|---|
| Top AppBar (sticky) | `_buildHeader()` |
| Section 1 — Liên kết & Phân loại | `_buildLinkSection()` |
| Section 2 — Thông tin hợp đồng | `_buildInfoSection()` |
| Section 3 — Mốc thời gian | `_buildTimelineSection()` |
| Section 4 — Giá trị (đồng) | `_buildValueSection()` |
| Section 5 — Điều khoản & Ghi chú | `_buildTermsSection()` |
| Bottom Action Bar | `_buildBottomBar()` |

### 2.2 Component Mapping

| Stitch Element | Flutter Widget | Notes |
|---|---|---|
| `<header>` sticky | `Container` with `BoxShadow` | top header |
| Card section button | `InkWell` + `AnimatedRotation` | collapse/expand |
| `<select>` | `DropdownButton<T>` | wrapped in bordered Container |
| `<input type="text">` | `TextField` | custom decoration |
| `<input type="date">` | `TextField(readOnly)` + `showDatePicker` | tap to open picker |
| `<input type="number">` | `TextField(keyboardType: number)` | currency/duration |
| `<textarea>` | `TextField(maxLines: ...)` | terms, notes |
| Submit button | `ElevatedButton` | fixed bottom bar |
| Material Symbols | `Icons.*` (Material) | arrow_back, more_vert, link, description, calendar_month, payments, sticky_note_2 |

### 2.3 Style Mapping

| Stitch Style | Code Token |
|---|---|
| Primary `#005f9e` | `ColorApp.color005F9E` |
| Background `#f7f9fb` | `ColorApp.colorF7F9FB` |
| Card `#ffffff` | `Colors.white` |
| Card-header `#e2f0fb` | `ColorApp.colorE2F0FB` |
| Outline `#c0c7d3` | `ColorApp.colorC0C7D3` |
| On-surface `#191c1e` | `ColorApp.color191C1E` |
| Error `#ba1a1a` | `ColorApp.colorBA1A1A` |
| Outline-grey `#707882` | `ColorApp.color707882` |
| Body text 14/w400 | `TextStyle(fontSize: 14, fontWeight: w400)` |
| Label 14/w500 | `TextStyle(fontSize: 14, fontWeight: w500)` |
| Header 18/w700 | `TextStyle(fontSize: 18, fontWeight: w700)` |
| Section title 14/w500 uppercase | `TextStyle(letterSpacing: 0.5, ...)` |

---

## 3. DATA MODELS

### 3.1 Entity Mapping

| Stitch/SRS Field | Model Class | Type |
|---|---|---|
| AddContract payload | `AddContractRequest` | POST body |
| filter-options | `FilterOptionsResponse`, `DeptLineModel`, `ProductRefModel` | dropdown depts + product refs |
| Opportunities | `OpportunityModel` | dropdown source |
| Projects | `ProjectRefModel` | dropdown source |
| Customers | `CustomerModel` | search input |
| Product services | `ProductServiceModel` | dropdown source |
| Static enums | `AddContractConfig` (regions, projectFields, contractTypes, contractStatuses) | hardcoded |

### 3.2 Static Enums (from SRS §4)

| Enum | Codes |
|---|---|
| Regions | ITKV1..5, HCMC, OVERSEAS, NGHEAN, DONGTHAP, DONGNAI, HANOI |
| Project Fields | EGOV, HEALTH, EDUCATION, FINANCE, TELECOM, ENTERPRISE, TRANSPORT, AGRICULTURE, OTHER |
| Contract Types | LEASE, DEPLOY, MAINTENANCE, UPGRADE, OTHER |
| Contract Statuses | DRAFT, SIGNED, ACTIVE, EXPIRING, EXPIRED, LIQUIDATED |

---

## 4. API MAPPING

| Action | Endpoint | Method | Service Method |
|---|---|---|---|
| Load filter options | `/api/v1/dashboard/filter-options` | GET | `getFilterOptions()` |
| Load opportunities | `/api/v1/opportunities?page=0&size=1000` | GET | `getOpportunities()` |
| Load projects | `/api/v1/projects?page=0&size=1000` | GET | `getProjects()` |
| Load customers | `/api/v1/customers?page=0&size=1000` | GET | `getCustomers()` |
| Load product services | `/api/v1/product-services?page=0&size=1000` | GET | `getProductServices()` |
| Submit new contract | `/contracts` | POST | `createContract()` |

---

## 5. STATE MANAGEMENT

```
AddContractScreen (extends BasePageStatefulWidget)
 └── _AddContractScreenState extends BaseStatefulWidgetState<..., AddContractService, AddContractProvider>
      └── buildBody() → BodyAddContractWidget (StatefulWidget)
           └── ConsumerBase<S,P> for loading/error state
           └── Provider state via context.read<AddContractProvider>()
```

---

## 6. BUSINESS RULES (from SRS §5)

| Rule | Implementation |
|---|---|
| BR-001 Auto-compute VNPT-IT | `AddContractProvider.computedVnptItValue` getter |
| BR-002 Inherit from Opportunity | `onOpportunitySelected()` |
| BR-003 Date range validation | `setSignedDate()` / `setExpiryDate()` set `expiryDateError` |
| BR-004 Filter products by owning unit | `filteredProductServices` getter |
| BR-005 Form validation before submit | `validate()` in provider, called from `submit()` |

---

## 7. FILE STRUCTURE OUTPUT

```
lib/features/add_contract/
├── add_contract_screen.dart             ✓
├── add_contract_mapping.md              ✓ (this file)
├── DEVELOPMENT_STATUS.md                ✓
├── component/
│   ├── add_contract_provider.dart       ✓
│   └── add_contract_service.dart       ✓
├── model/
│   └── add_contract_model.dart          ✓
└── widget/
    └── body_add_contract_widget.dart    ✓
```

---

## 8. ROUTE INTEGRATION

- Route key: `RouteGenerator.addContract = '/add-contract'`
- Route registered in `lib/core/constants/route_generator.dart`
- Wrapped with `MultiProvider` (provider + service) — Dart instance from `DioOption().createDio()`
- Navigation after successful save: `Navigator.pushNamedAndRemoveUntil(context, RouteGenerator.listContractReal, ...)`

---

## 9. REVIEW CHECKLIST

- [x] Mapping covers all 5 SRS sections + 4 filter APIs
- [x] Material Symbols mapped to Material Icons
- [x] All `AppStrings.*` used (no hardcoded Vietnamese)
- [x] `ColorApp.*` used (no hex literals in widget)
- [x] Provider extends `BaseProvider`, screen extends `BasePageStatefulWidget`
- [x] Body widget is `StatefulWidget` (form controllers need `dispose()`)
- [x] `mounted` checked after `await` in save handler
- [x] `const` on `const`-eligible constructors
- [x] Hard rules: BR-001..005 implemented